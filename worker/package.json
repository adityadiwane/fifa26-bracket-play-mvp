{
  "name": "fifa26-bracket-play-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply:local": "wrangler d1 execute fifa26-bracket-play-db --local --file ../db/schema.sql",
    "db:apply:remote": "wrangler d1 execute fifa26-bracket-play-db --remote --file ../db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
