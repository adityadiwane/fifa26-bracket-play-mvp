# Data files

Use `matches.sample.csv` as the format for a real `matches.csv` file.

Required outcome values:

- `HOME_WIN`
- `DRAW` — group stage only
- `AWAY_WIN`

For knockout placeholder rows, leave `home_team` / `away_team` blank and fill `home_placeholder` / `away_placeholder`.
